mixin loginForm(store = {}, method = 'POST')
  form.form(action="/login" method=method)
    h2 Login
    label(for="name") Email
    input(type="text" name="email" value="wesbos@gmail.com")
    label(for="name") Password
    input(type="password" name="password" value="wes")
    input.button(type="submit" value="Log In → ")
